# Repository documentation (not deployed to home)
CLAUDE.md
README.md
CHANGELOG.md

# Tmux plugins are managed by TPM, not tracked in dotfiles
.config/tmux/plugins/

{{- if ne .chezmoi.os "darwin" }}
Brewfile
{{- end }}

{{- if ne .chezmoi.os "linux" }}
# Hyprland compositor (Linux desktop only)
.config/hypr/

# systemd user services (Linux only)
.config/systemd/

# Linux-specific scripts
bin/kbdlightd
bin/set-kbd-brightness
bin/compact_workspaces.sh

# System configuration files (require manual installation)
etc/logind.conf

# Arch Linux package list
.config/pkglist.txt
{{- end }}

{{- if eq .chezmoi.os "linux" }}
# System configuration files are tracked but require manual installation
# See SYSTEM_FILES.md for installation instructions
etc/

# macOS-specific files (launchd services)
Library/
{{- end }}
create-mtp-udev-rule.sh

# Claude credentials contain OAuth tokens and should be encrypted with age
# TODO: Set up age encryption and add with: chezmoi add --encrypt ~/.claude/.credentials.json
.claude/.credentials.json

# Claude ephemeral/cache data (not needed in dotfiles)
.claude/debug/
.claude/todos/
.claude/shell-snapshots/
.claude/history.jsonl
.claude/projects/
.claude/file-history/
.claude/session-env/
.claude/local/
.claude/statsig/
